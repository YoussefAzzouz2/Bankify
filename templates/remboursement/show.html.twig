{% extends 'base.html.twig' %}

{% block title %}Remboursement{% endblock %}

{% block body %}
    <h1>Remboursement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ remboursement.id }}</td>
            </tr>
            <tr>
                <th>MontantR</th>
                <td>{{ remboursement.montantR }}</td>
            </tr>
            <tr>
                <th>MontantRestant</th>
                <td>{{ remboursement.montantRestant }}</td>
            </tr>
            <tr>
                <th>DateR</th>
                <td>{{ remboursement.dateR ? remboursement.dateR|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DureeRestante</th>
                <td>{{ remboursement.dureeRestante }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_remboursement_index') }}">back to list</a>

    <a href="{{ path('app_remboursement_edit', {'id': remboursement.id}) }}">edit</a>

    {{ include('remboursement/_delete_form.html.twig') }}
{% endblock %}
