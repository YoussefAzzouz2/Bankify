{% extends 'base.html.twig' %}

{% block title %}Liste Assurance{% endblock %}

{% block body %}
    <h1>Liste des Assurances</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ assurance.id }}</td>
            </tr>
            <tr>
                <th>Type d'Assurance</th>
                <td>{{ assurance.typeAssurance }}</td>
            </tr>
            <tr>
                <th>MontantPrime</th>
                <td>{{ assurance.montantPrime }}</td>
            </tr>
            <tr>
                <th>Nomde l'Assuré</th>
                <td>{{ assurance.nomAssure }}</td>
            </tr>
            <tr>
                <th>Nom du Beneficiaire</th>
                <td>{{ assurance.nomBeneficiaire }}</td>
            </tr>
            <tr>
                <th>Info sur Assurance</th>
                <td>{{ assurance.infoAssurance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assurance_index') }}">back to list</a>

    <a href="{{ path('app_assurance_edit', {'id': assurance.id}) }}">edit</a>

    {{ include('assurance/_delete_form.html.twig') }}
{% endblock %}
