{% extends 'backbase.html.twig' %}

{% block title %}<title>Edit Carte</title>{% endblock %}

{% block content %}
    <div style="display: flex; justify-content: center;">
    <div>
    <h1>Modifier Carte</h1>

    <center>{{ include('carte/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_carte_index') }}">back to list</a>

    {{ include('carte/_delete_form.html.twig') }}</center>
    </div>
    </div>
{% endblock %}
