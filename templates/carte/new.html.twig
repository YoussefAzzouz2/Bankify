{% extends 'backbase.html.twig' %}

{% block title %}<title>New Carte</title>{% endblock %}

{% block content %}
    <div style="display: flex; justify-content: center;">
        <div>
            <h1>Create new Carte</h1>

            {{ include('carte/_form.html.twig') }}

            <a href="{{ path('app_carte_index') }}">back to list</a>
        </div>
    </div>
{% endblock %}
