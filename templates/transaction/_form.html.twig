{{ form_start(form) }}
    <div class="form-group">
        {{ form_label(form.Montant) }}
        {{ form_widget(form.Montant, {'attr': {'class': 'form-control'}}) }}
        <div class="form-errors-wrapper">
        {{ form_errors(form.Montant) }}
    </div>
    </div>

    <div class="form-group">
        {{ form_label(form.Date_T) }}
        {{ form_widget(form.Date_T, {'attr': {'class': 'form-control'}}) }}
        <div class="form-errors-wrapper">
        {{ form_errors(form.Date_T) }}
    </div>
    </div>

    <div class="form-group">
        {{ form_label(form.Type_T) }}
        {{ form_widget(form.Type_T, {'attr': {'class': 'form-control'}}) }}
        <div class="form-errors-wrapper">
        {{ form_errors(form.Type_T) }}
    </div>
    </div>

    <div class="form-group">
        {{ form_label(form.Statut_T) }}
        {{ form_widget(form.Statut_T, {'attr': {'class': 'form-control'}}) }}
        <div class="form-errors-wrapper">
        {{ form_errors(form.Statut_T) }}
    </div>
    </div>

    <!-- Add CSS to create space between form fields -->
    <style>
        /* Adjust spacing between form fields */
        .form-group + .form-group {
            margin-top: 20px;
        }
    </style>

    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
