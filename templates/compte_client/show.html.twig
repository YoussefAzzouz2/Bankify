{% extends 'backbase.html.twig' %}

{% block title %}<title>CompteClient</title>{% endblock %}

{% block content %}
    <h1>CompteClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compte_client.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ compte_client.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ compte_client.prenom }}</td>
            </tr>
            <tr>
                <th>Rib</th>
                <td>{{ compte_client.rib }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ compte_client.mail }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ compte_client.tel }}</td>
            </tr>
            <tr>
                <th>Solde</th>
                <td>{{ compte_client.solde }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_compte_client_index') }}">back to list</a>

    <a href="{{ path('app_compte_client_edit', {'id': compte_client.id}) }}">edit</a>

    {{ include('compte_client/_delete_form.html.twig') }}
{% endblock %}
