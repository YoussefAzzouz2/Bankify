{% extends 'base.html.twig' %}

{% block title %}Agence{% endblock %}

{% block body %}
    <h1>Agence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ agence.id }}</td>
            </tr>
            <tr>
                <th>Nom_agence</th>
                <td>{{ agence.nomAgence }}</td>
            </tr>
            <tr>
                <th>Email_agence</th>
                <td>{{ agence.emailAgence }}</td>
            </tr>
            <tr>
                <th>Tel_agence</th>
                <td>{{ agence.telAgence }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_agence_index') }}">back to list</a>

    <a href="{{ path('app_agence_edit', {'id': agence.id}) }}">edit</a>

    {{ include('agence/_delete_form.html.twig') }}
{% endblock %}
